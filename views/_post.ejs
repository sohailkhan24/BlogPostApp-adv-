<li>
  <p>
    <div class="p-1 font-italic "><h6><%= post.title %></h6></div>
    <br />
    <small> Created by-<%= post.user.name %> </small><br />
    <small>Created At- <%= post.user.updatedAt %> </small><br />
    <% if(locals.user && locals.user.id == post.user.id){ %>
    <small>
      <a href="/posts/destroy/<%=post.id%>"><i class="far fa-trash-alt"></i></a>
    </small>
    <% } %>
  </p>
  <div class="post-comments">
    <div>
      <ul>
        <%for(comment of post.comments){%> <%-include('_comment')-%> <%}%>
      </ul>
    </div>
    <% if (locals.user){ %>
      <div class="ml-5">
        <form action="/comments/create" method="POST">
          <input
            type="text"
            name="content"
            placeholder="Type Here to add comment..."
            required
          />
          <input type="hidden" name="post" value="<%= post._id %>" />
          <input type="submit" value="Add Comment" />
        </form>
      </div>
    

    <% } %>
   
  </div>
</li>

<!-- <div class="container"> -->
<!-- <div class="row"> -->
<!-- <link
  rel="stylesheet"
  href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css"
  integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO"
  crossorigin="anonymous"
/>
<div class="col-md-1"></div>
<div class="col-md-6">
  <div id="postlist">
    <div class="panel">
      <div class="panel-heading">
        <div class="text-center">
          <div class="row">
            <div class="col-sm-9">
              <h3 class="pull-left"><%= post.title %></h3>
            </div>
            <div class="col-sm-3">
              <h4 class="pull-right">
                <small
                  ><em>2014-07-30<br />18:30:00</em></small
                >
              </h4>
            </div>
          </div>
        </div>
      </div>

      <div class="panel body">
        <div
          class="text-center text-justify bg-primary text-wrap"
          style="width: 8rem; text-overflow: inherit"
        >
          <p class="text-justify bg-primary text-wrap"><%= post.content %></p>
          <% if(locals.user && locals.user.id == post.user.id){ %>
          <small>
            <a href="/posts/destroy/<%=post.id%>">X</a>
          </small>
          <% } %>
        </div>
      </div>

      <div class="panel-footer">
        <span class="label label-default"><%= post.user.name %></span>
        <span class="label label-default">Edit</span>
        <span class="label label-default">like</span>
        <div class="post-comments">
          <% if (locals.user){ %>
          <form action="/comments/create" method="POST">
            <input
              type="text"
              name="content"
              placeholder="Type Here to add comment..."
              required
            />
            <input type="hidden" name="post" value="<%= post._id %>" />
            <input type="submit" value="Add Comment" />
          </form>

          <% } %>
          <div>
            <ul>
              <%for(comment of post.comments){%> <%-include('_comment')-%> <%}%>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- </div> -->
<!-- </div> -->
<!-- </div> -->
