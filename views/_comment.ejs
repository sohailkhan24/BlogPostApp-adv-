<li>
  <p>
    
    <div class="p-0 font-italic"><h6><%=comment.content%></h6></div>
    <small>By- <%=comment.user.name %> </small>
    <% if(locals.user && (locals.user.id == comment.user.id || locals.user.id ==
    post.user.id)){ %>
    <small>
      <a href="/comments/destroy/<%=comment.id%>"
        ><i aria-hidden="true" style="color:#00cc6a" class="far fa-trash-alt"></i
      ></a>
    </small>
    <% } %>
  </p>
</li>
